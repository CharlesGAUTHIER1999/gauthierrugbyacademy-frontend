services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: gauthierfitness_front_web
    ports:
      - "5173:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"